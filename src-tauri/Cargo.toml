[package]
name = "mirror-relu"
version = "0.1.0"
description = "顔まわりの無意識な動作に気づくためのローカル推論デスクトップミラー"
authors = ["azumag"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "mirror_relu_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
