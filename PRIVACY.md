# プライバシー方針

Mirror Re:lu は、Webカメラを常時利用し得る性質を踏まえ、映像を外へ出さないことを設計上の最優先事項とします。

## 取得する情報

アプリが扱う情報は次のとおりです。

- Webカメラの映像フレーム
- 顔・手の特徴点と表情係数
- 検出イベントの日時、種類、継続時間、確度、数値指標
- 感度、通知時間、選択カメラなどの設定
- 正面注視時の本人基準

## 保存する情報

既定では、検出イベント、設定、本人基準だけを端末内の WebView ストレージへ保存します。画像、動画、音声、フレーム、顔特徴点列、手特徴点列は保存しません。

履歴と設定はアプリ画面から削除できます。JSON書き出しはユーザーが明示的に操作した場合だけ実行されます。

## ネットワーク通信

実行時のアプリは、Content Security Policy により外部の `connect-src` を許可しません。推論モデルと WASM は開発・ビルド時に取得し、製品へ同梱します。

依存パッケージのインストール、モデルの取得、更新確認、CIなど、開発工程では外部通信が発生します。これらはカメラ映像を扱う実行時処理とは別です。

## MediaPipe

MediaPipe Tasks Vision はアプリ内に同梱したモデルを使い、Web Worker 内で推論します。本アプリはMediaPipeが生成した特徴量だけをアプリ内で利用し、Googleその他のサーバーへ送る処理を実装しません。

上流ライブラリの仕様やプライバシー条件が変わる可能性があるため、依存バージョン更新時はリリースノートと通信挙動を再確認します。

## 医療情報

検出履歴や本人基準は医療上の測定値ではありません。診断目的で第三者へ提出することを想定していません。医療相談に利用する場合も、アプリの値だけで判断せず、症状を医療従事者へ直接伝えてください。

## 問い合わせ・変更

プライバシー上の懸念は、このリポジトリのIssueで報告してください。脆弱性や、映像・個人情報が外部へ漏れる可能性を含む内容は、公開Issueではなく [SECURITY.md](./SECURITY.md) の手順を利用してください。
