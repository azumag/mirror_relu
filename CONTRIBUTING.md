# コントリビューション

## 基本フロー

1. Issueで目的と完了条件を明確にします。
2. 小さなブランチで変更します。
3. 検出ロジックを変更する場合は、同じPRにテストを追加します。
4. `npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml` を実行します。
5. カメラ権限、停止操作、履歴削除、CSPを手動確認します。

## 受け入れ基準

- 単一フレームで通知しない
- 通知後にクールダウンがある
- 映像・特徴点を保存しない
- 新しい外部通信先を追加しない
- 医療上の断定表現を追加しない
- 設定値に上限・下限がある
- 顔未検出、カメラ拒否、モデル失敗を安全に処理する

## コードスタイル

- TypeScriptはstrictモードを維持します。
- DOM更新に不特定な外部文字列の`innerHTML`を使いません。
- 数式は`src/core`へ置き、UIやWorkerから分離します。
- Workerへ渡した`ImageBitmap`は必ず閉じます。
- 検出器は共通の`TemporalGate`を利用します。
